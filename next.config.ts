/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
  images: { unoptimized: true },
  basePath: "/PawPal-Landig",
  assetPrefix: "/PawPal-Landig/",
};

module.exports = nextConfig;
